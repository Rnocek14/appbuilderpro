node_modules
dist
.env
.env.local
*.local
.DS_Store

supabase/.temp
.tmp-kitcheck/
.design-e2e/
_pdf_streams.txt
*.tsbuildinfo
test-results/
playwright-report/
e2e/.auth/
/blob-report/
.playwright/

# transient Playwright drive scripts written by test agents at repo root
/*.mjs

# scratch/demo artifacts (throwaway HTML, screenshots, one-off scripts)
scratchpad/

# Live-fetched prospect pages (scan:live --keep) — real third-party HTML, not ours to commit
scripts/scanProbe/fetched/
dist-drift
