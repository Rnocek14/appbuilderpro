# 14 — Open Decisions: The Dispute Ledger and Decision Register

*Phase 3, Experience Architecture. This is the file the constitution's preamble and §16, and
13 §0, route every inter-document disagreement and threshold revision to. The rule it enforces:
when a document author believes a binding decision fails a scenario, or two documents specify
the same thing differently, the conflict is logged here and adjudicated here — never resolved
by quiet local deviation. An entry stays open until its resolution is written back into the
owning documents; closing an entry without those edits is itself a defect.*

**Entry format.** Each entry carries: status (**open** / **resolved** / **held** — held means
deliberately deferred, not disputed), the documents it touches, the question stated neutrally,
the competing options with their real trade-offs, and — the register's second job — **what
evidence would settle it**, using the instruments in §5 (P = prototype test, O = operator
interview, M = instrumented pilot metric, R = ruling by the owning authority). Numbering is
append-only and stable: documents cite entries as OD-NN and those anchors must never move.

**Status tally:** 59 entries — 43 open · 12 resolved (rulings recorded below) · 4 held.

---

## 1. The register

### Carried from the fix wave (OD-01 – OD-16)

#### OD-01 · The conversation surface — obligations not yet landed
**Status:** open. **Touches:** constitution §8; 03 (owes the anatomy section); 12 (owes the frame); 11 (`/w/:world/t/:thread`).
**Question:** Constitution §8 binds the two thread kinds and the conversation surface's shape;
03 still lacks the owning anatomy section (message artifacts on the trace, staged reply at the
foot, per-message channel stamps, heartbeat chips, resume/since-then) and 12 lacks the
conversation wireframe. Not a dispute — an unlanded obligation, the largest one left.
**Closes when:** 03 lands the section and 12 lands the frame, both consistent with §8.

#### OD-02 · Artist-world spec fork (mural scenario)
**Status:** resolved. 10 §3.2 and Appendix A now carry 03 §7.3's dressing verbatim — six areas
including Outreach, the Business reading, the wrong-Client charter corrected in §3.5 as the
sanctioned wrong-genome beat. Residual cite-level edits (03 §7.3 acknowledgment line; grant-actor
re-staging in 03 §7.4; 07 §7.2 grant-actor rule) are in the editorial queue (§4).

#### OD-03 · Scenario numbering vs journey numbering
**Status:** open, narrowed. **Touches:** 01 (mapping table, landed); 10; 04 §16; 13 AT-09.
**Question:** 01 now publishes the S↔J mapping (S-numbers stable, "Rendered in 10 as" column).
Remaining: 10 owes the reciprocal table in Appendix A; 04 §16's "10 (S6 and S7 as full
journeys)" is still wrong for S7 (S7 is distributed across day-30 beats, owned by AT-09).
**Partial ruling:** the S6 rabbit-hole subject is **bee-hives** (01, 04, 13 AT-09 — three
documents against one); 10 J6 either re-subjects or footnotes its coffee-cart as an independent
exemplar, Rossi-style. **Evidence:** none needed — R, then the two edits.

#### OD-04 · The re-genome operation has no owner
**Status:** open. **Touches:** constitution §7, §11; 04 §10; 09; 10 §3.5, §6.5; 03 (dressing).
**Question:** Promotion, demotion, and kind-correction all mutate the genome stack under a live
world, and no document owns the operation: what happens to existing dressing, areas, memory,
and a running watch; how old and new dressing conflicts resolve; and — the constitutional gap
04 flagged — whether **shedding** layers is symmetric with adding them (04 asserts one-confirm
identity-preserving demotion; the constitution only ever describes adding).
**Options:** (a) 09 owns all stack mutations as one chapter (creation is genesis and
re-genesis) — cleanest ownership, but 09 is already the longest ceremony doc; (b) 04 owns
promotion, 09 owns demotion/correction — matches where the journeys live, splits one operation
across two docs; (c) 03 owns it as world grammar — keeps dressing rules adjacent, buries a
lifecycle event in a structure doc. **Proposed:** (a), with 03 owning only conflict *rendering*.
**Evidence:** R (assign the owner), then P — walk 10 §3.5 and §6.5 against the drafted rule and
check nothing the journeys exercised is forbidden.

#### OD-05 · S5 inbound/outbound identity split
**Status:** resolved. S5 **is** the inbox automation, rendered as Journey 5b (10 §5.6–5.10,
added end-to-end); J5a (Podcast Outreach) is its outbound sibling, coverage-bearing in its own
right. 01's S5 row names both threads and cross-references this entry. No residue.

#### OD-06 · Consent-gate scope for cold email
**Status:** resolved. 05 §9.1 defines the send-class boundary: recorded-consent classes fail
closed; cold first-touch exists as a sanctioned hand-gated class in which **first contact with a
never-before-contacted recipient is structurally ineligible for autonomy** — 10 J5a states the
same rule (§5.3, line-for-line intent). Residuals (16 §13.2 names the class; 05 §8.3 states the
"never-before-contacted" definition explicitly) are in the editorial queue.

#### OD-07 · The light Automation-kind world
**Status:** open. **Touches:** constitution §11; 09; 10 §5.1; 03; 11; 15 (eventually).
**Question:** J5 births "Podcast Outreach" from a Tab-resolved chip with no Proposal. Which
ceremony rung is that, and how light is the Automation display-kind — does it carry Face
health? areas beyond one studio? a Playbook?
**Options:** (a) a named light rung between curiosity and venture (chip-Tab birth) — honest
about what happened, but a fourth rung complicates a ladder the constitution states in four
steps; (b) it *is* the venture rung and the Tab is the one confirm — no ladder change, but a
chip-Tab is a thin "confirm" for a world that will send email; (c) require the mini-Proposal —
consistent, but taxes the lightest recurring work with ceremony. **Proposed:** (b), with the
chip expanded inline to show cost/cadence before Tab commits (the confirm becomes honest
without becoming a screen). **Evidence:** P — creation-timer test against 13's 5s/60s bounds;
M — fraction of light worlds needing re-genome within 60 days (if high, (c) was right).

#### OD-08 · Dormancy and decay defaults
**Status:** resolved. 04 §9.2 owns the clock and its defaults are the contract: two quiet weeks
to cooling, six to dormant, tunable per world; 08 §4.3 cites it and scopes dormancy by genome
(client worlds sleep only via explicit retirement; outbound/spending/counterparty clock work
blocks dormancy); 13 AT-05 idles two months. The 60-day prospect aging-out default (08 §3.2)
moves to the threshold register (OD-59).

#### OD-09 · Landing defaults — cold returns, curiosity flips, needs-you entries
**Status:** open, extended. **Touches:** constitution §5 ("default landing"); 02 §7.2; 03; 04
§12; 11 §6.3; 12 W1.
**Question:** Three variants of one rule. (1) Cold return: Desk-with-since-then vs the exact
surface left (map/Ledger story). (2) The curiosity flip: 02 §7.2 lands curiosity worlds on the
map (04 agrees; "the map IS the record") — but constitution §5's letter names the Desk every
world's default landing, so the carve-out needs the amendment queue (§3). (3) Warm entry: a
Field orb glowing needs-you opens the Desk (02's call, for predictability) vs opening with
Queue items foregrounded.
**Options:** one uniform Desk rule (predictable, but wastes the return moment for curiosity
worlds and slows triage for needs-you) vs per-kind/per-state exceptions (right thing each time,
but landing becomes something operators must model). **Evidence:** P — S7 return test: time to
re-orientation against AT-09's two-minute bound under each rule; M — first-action latency after
needs-you entry (if operators' first act after Desk landing is opening the Queue >70% of the
time, the Queue-foregrounded variant wins); then R for the §5 amendment.

#### OD-10 · The watch/dormancy deadlock — and the watch itself
**Status:** resolved. **Ruling:** the inbound-only watch on an unpromoted rabbit hole is
**ratified** (04 §9.3's deliberate exception to "curiosity runs nothing": sends nothing,
touches no counterparty, spends within its stated cap, mounted via the standard one-confirm
path). It is the one named exception to the clock-work dormancy bar: it **sleeps with its
world** (08 §4.3), arrivals accumulate for the return (04 §12), the watch stays a Running-lens
row with heartbeat and went-quiet mechanics, and a watch failure files to the Queue and invokes
the uncompressible rule. Residual: 04 §9.3 owes the explicit one-line "sleeps with its world"
mirror sentence 08 already claims of it (editorial queue).

#### OD-11 · Cross-world adjacency surfacing
**Status:** resolved. **Ruling, two parts:** (1) re-entry adjacency surfaces **gate-promoted,
scrubbed pattern cards only** (04 §12, per constitution §12.5/§13); (2) the portfolio index may
additionally surface **adjacency pointers** (17 §9.1's named carve-out): operator-only,
provenance-stamped signposts between the operator's own worlds — a pointer opens in its home
world, is never a payload, never renders counterparty-derived rows outside that world, never
appears on counterparty-visible surfaces, never enters another world's grounding. Acting on one
still requires mention+grant or §7 promotion. Residuals: 01 P11 gains the one-line
qualification (learned things travel gate-promoted and scrubbed, or as pointers); 13 formalizes
the extended scrub test (17 §10.7). Both in the editorial queue.

#### OD-12 · Document 15 pending — bindings held open
**Status:** held. 13's bindings on the master blueprint (AT-01, the global bindings) stay open
until 15 is written, deliberately last. **Two rulings recorded:** the filename is
`15-master-blueprint.md` — the constitution's name wins over the task list's longer variant.
And the missing phase-brief text (02's unresolvable flag: the brief's verbatim question lists
exist nowhere in the repo) is answered in 15 as a brief-question → doc-section **coverage
matrix**, once the repo owner supplies the brief; no document fabricates the lists meanwhile.

#### OD-13 · Publish-state machine (constitution §9 vs 07 §2.4)
**Status:** resolved. 07 §2.4 now carries a genuine **Approved** state (the gate said yes;
execution in flight — a moment or an hour, but always real), so the machine reads Draft →
Waiting → Approved → Live → Superseded/Retired and matches §9's draft → approved → live spine
exactly, with Waiting/Superseded/Retired as elaborations. No amendment needed. Residual: 12 W8
must render Waiting as the staged state and show Approved where a publish has duration
(editorial queue).

#### OD-14 · Scope-chip gesture — 11 §10.9's amendment of 02 §3.3
**Status:** open. **Touches:** 02 §3.3 rule 3; 11 §7, §10.9; 12 W1.
**Question:** 11 §10.9 rules the chip summons the switcher, with the standing-scope override as
the switcher's per-row "address, don't go" action — explicitly amending 02 §3.3 rule 3, which
still reads "click the chip and pick" (verified unchanged). The deeper rule is undisputed:
nothing but an explicit act changes standing scope. **Closes when:** 02 §3.3 rule 3 is
rewritten to the switcher reading and 12 W1 shows the row action. **Evidence:** none — R;
the resolution is chosen, only the 02/12 edits are owed.

#### OD-15 · The cross-scope read-answer affordance (AT-02)
**Status:** open. **Touches:** 13 AT-02 ("How's Rossi doing?" from inside the apparel world);
02 §3 (fire-and-stay, §3.7); 08; constitution §13.
**Question:** AT-02 requires an in-place, read-only, evidence-linked answer about another world
with no navigation and no scope change; no document specifies the affordance.
**Proposed shape:** a transient, world-stamped answer card in 02's fire-and-stay family —
renders rows only within the asked world's scope, stamped with that world; anything beyond
reading routes normally. 02 owns it; 08 cites it.
**Evidence:** P — isolation red-team: can a session in world A be steered into rendering world
B counterparty content via chained read-answers? The card ships only if the red-team fails.
O — "did you trust that number without visiting the world?" (trust without verification is the
point; trust with suspicion means the stamps are too weak).

#### OD-16 · Birth carve-out to the 3–7 area band (constitution §5 vs 03 §4.4)
**Status:** open. **Touches:** constitution §5; 03 §4.4, §7.6; 16 §13.5.
**Question:** Does the 3–7 visible-areas band bind at birth or at operating maturity? 03 reads
it as an operating band — a newborn curiosity world shows one area (its map), because mounting
furniture onto a silent birth is ceremony by structure (§11: a rabbit hole costs nothing).
**Proposed amendment** (queued in §3): "up to 7 visible; operating genomes charter with 3–5;
newborn/curiosity worlds may show fewer." **Evidence:** R — this is a pure constitutional
reading; no prototype changes it. Closes when §5 is amended and 03 §4.4 repoints its cite.

### Shell, attention, and the portfolio (OD-17 – OD-21)

#### OD-17 · Mobile approval integrity
**Status:** open. **Touches:** constitution §14; 01 P1; 02 §9, §11; 06; 12 §13; 16 §11; 13.
**Question:** Mobile is approve-heavy by design, and P1 says mastery must not decay into
rubber-stamping on a phone. Three sub-questions, one theme. (1) Should high-weight approval
classes occasionally surface the class's criteria/rubric inline *before* the approve gesture —
a judgment-preserving interstitial — or is full inline context enough? (2) Should the Brief's
needs-you stanza carry one-tap approve on mobile (fast, but duplicates the Queue's action
surface) or keep linking into the Queue? (3) Tournament walks: pairwise compare is in mobile
scope, so a Queue-style j/k tournament walk is feasible — does it join the sanctioned mobile
states, or do workshops stay desktop-only beyond review/critique/approve?
**Trade-offs:** every added mechanic taxes the calm operate-mode; every omission risks the
phone becoming a yes-button. **Evidence:** M — approval dwell time and overturn/regret rate by
class, mobile vs desktop (if mobile dwell collapses below a class's desktop baseline while
overturns rise, the interstitial earns its friction); P — j/k tournament on a phone with real
variant pairs; O — "which of this week's mobile approvals would you decide differently at a
desk?"

#### OD-18 · Stewardship surfaces in the information architecture
**Status:** open. **Touches:** 01 P2; 02; 03 (Face grant lists); 11.
**Question:** Grant management, credentials, and contacts serve *trust*, not any metabolism
beat; 01 deliberately did not force them into the loop. Does 11 name them a sixth surface
category ("stewardship surfaces") or treat them as overlays of existing beats?
**Options:** named category (honest IA, but a new top-level concept for surfaces operators
visit rarely) vs overlays on the Face/Queue (no new concept, but revocation and audit paths
stay scattered). **Evidence:** O + P — tree test: "revoke the brokerage's access to Mom's
listings" and "what can the clothing world see of Marco's?"; if operators find the answer in
under 30 seconds from the Face in both directions, overlays suffice.

#### OD-19 · Consolidating the everything view
**Status:** open. **Touches:** 01 P4; 02 §7; 04 §11.5 (fold-in); 08 §4.4; 09 §12.
**Question:** Silent lazy birth means the everything view accumulates micro-explorations the
operator never consciously created. The fold-in verb (04 §11.5, operator gesture) and 09 §12's
system-proposed merge exist; unresolved is whether a **periodic consolidation digest**
(merge-suggestions for overlapping explorations) should run proactively.
**Options:** periodic digest (tidy census, but proposal traffic against P3 — see OD-58) vs
suggestions only on entering the everything view (zero ambient cost, helps only those who
census) vs manual-only. **Evidence:** M — count of overlapping dormant explorations per
operator at day-90; O — show the census: "does this pile bother you?" If operators shrug, P4's
decay answer was already sufficient.

#### OD-20 · Quiet hours vs decaying approvals
**Status:** open. **Touches:** 02 §9; 06 §3.4; 13 AT-10.
**Question:** 02 rules only the stale-clock push crosses quiet hours, so a decaying
speed-to-lead approval can silently expire overnight — calm traded against revenue.
**Options:** calm-absolute (current ruling; predictable nights, lost leads) vs an operator-set
may-wake-me mark per approval class (surgical, but a new config surface on the most
trust-sensitive machinery) vs decay clocks pausing during quiet hours (no loss, no wake — but
counterparty-side reality doesn't pause, so the "expiry" becomes theater).
**Evidence:** M — count and value of approvals that expired inside quiet hours over a pilot
month; O — "which of these expirations would you have wanted to be woken for?" If the answer is
"none," the ruling stands; if it clusters in one class, the per-class mark wins narrowly.

#### OD-21 · IA mechanics bundle
**Status:** open. **Touches:** 11 §4–§7, §9; 12; 17 §10.
**Question:** Three deferred mechanics. (1) Cold-opened `/queue/:item` from a push renders over
Home (11's call) vs over the item's own world (primes context, costs load time and surprise).
(2) Bare area segments (`/w/:world/:area`, reserved-word list) vs typed prefix
(`/w/:world/a/:area`) — readability vs reserved-list maintenance as the set grows. (3) The
calibration view lives as a Playbook anchor (`#calibration`) vs its own reserved view.
**Evidence:** (1) P — cold-open both variants on real approvals; measure decision quality and
back-out rate. (2) R — engineering call once the reserved set is enumerated; if it exceeds ~12
words, take the prefix. (3) M — calibration-view entry paths in pilot; if operators reach it
from Observe staging far more than from the Playbook, promote it to a view.

### World grammar and genome behavior (OD-22 – OD-24)

#### OD-22 · Layer-stack conflict resolution — stances and criteria packs
**Status:** open. **Touches:** 03 §6 (counsel merging); 16 §7 (packs name their version);
09 §10; operating model §8.3 (format deferred to engineering); 17 §5.
**Question:** Under 3+ stacked operating layers, 03 rules the heaviest layer's stance leads
while lighter layers survive as postures of it — untested for client+brand+product stacks. The
sharper half: when stacked layers each supply a criteria pack for the same craft, which pack
wins and how versions merge — the workshop promise "every score names its pack version" cannot
hold at layer boundaries without a merge rule.
**Options:** generalize 03's heaviest-leads rule to packs (one rule everywhere; risks burying a
local override under an inherited base) vs an explicit pack-priority list on the Proposal
(visible, editable; one more Proposal row) vs local-always-wins with inherited packs as shadow
columns in critique (preserves both signals; widens every scorecard).
**Evidence:** P — run one critique session over a three-layer stack with deliberately
conflicting packs; operators must answer "why this score?" correctly. The option under which
they can is the answer. Engineering's genome-format spike (operating model §8.3) constrains
the implementable set; R closes it.

#### OD-23 · Arrival posture — sticky vs auto-flip
**Status:** open. **Touches:** 03 §5; 04 §12; 10 (day-30 beats); 13 AT-09.
**Question:** 03 chose sticky-last-posture plus a one-line suggestion chip ("results came in —
Observe?") over situation-driven auto-flip.
**Trade-offs:** sticky is predictable and never wrong-foots the operator; auto-flip serves the
return moment but makes the room rearrange itself. **Evidence:** M — chip acceptance rate after
absences longer than a week; if it falls under ~20%, the chip is noise and auto-flip-with-one-
tap-return gets its fair trial in the S7 journey test (P).

#### OD-24 · Stage-driven vitals — veto or pin
**Status:** open. **Touches:** 03 §2.5.2; constitution §11 (inheritance proposals); 09 §10.
**Question:** When a genome retires launch-runway for sell-through at a stage change, can the
operator permanently veto the swap, or only pin the old vital alongside?
**Options:** pin-only (the genome's judgment stays authoritative; the Face may carry a stale
instrument the operator resents) vs full veto as a local diff (operator sovereignty, consistent
with §11's local-override grammar; a world can drift blind to its stage). **Proposed:** veto as
a local diff — it is exactly what §11's customization grammar already permits everywhere else.
**Evidence:** O at stage transitions ("did the Face change under you? did you want it back?");
M — pin usage count. R ratifies.

### Explore (OD-25 – OD-27)

#### OD-25 · Fork-on-pivot etiquette
**Status:** open. **Touches:** 04 §5; 02 §3 (routing philosophy); 12 (Explore frame).
**Question:** Sustained topic pivots fork silently with a named, undoable delta chip (04's
ruling, matching routing) vs always offering a fork chip before forking.
**Trade-offs:** silent forking risks map fragmentation the operator must undo; offer-first
interrupts the conversation that Explore exists to protect. Only prototyping settles which
error is cheaper. **Evidence:** P — seeded pivot script over both variants; M — undo rate of
silent forks vs offer-decline rate. The variant whose error is rarer *and* cheaper to repair
wins; ties go to silence (P4).

#### OD-26 · A third evidence class on the map
**Status:** open. **Touches:** 04 §6; 07 §5 (research-map snapshots); 16 (theory workshop).
**Question:** "supports (in simulation)" already separates model-derived from world-derived
evidence. Does AI-synthesized reasoning with no external source need its own third class, or
does that granularity clutter the map?
**Evidence:** P — map-legibility test: operators sort 20 evidence edges by trustworthiness
with and without the third class; M — how often synthesis is mistaken for sourced evidence in
the two-class rendering. If the confusion rate is near zero, two classes stand.

#### OD-27 · The metadata/content boundary for cross-world references
**Status:** open, half-resolved. **Touches:** 16 §4.2 (the ruling); 04 §14 (provenance stubs);
10 §J4.5; 13 AT-15; 02 §3.5; 07 §7.1; constitution §13.
**Resolved half:** 16 §4.2's reference-listing rule stands — operator-owned sibling-world
material lists metadata-only (greyed, unplaceable, sole action "request a grant");
counterparty-bound material never lists, search simply misses.
**Open halves:** (1) provenance-stub rendering — exactly how much a stub may reveal about what
it points to without leaking (title? kind? owner? thumbnail — surely not); (2) the crisp
definition of "counterparty-visible surface" that AT-15's provenance-depth rule needs (a draft
email body is counterparty-visible; is the workshop around it?).
**Evidence:** P — leakage red-team: an adversarial reader of every stub rendering and every
"counterparty-visible" candidate surface tries to reconstruct the other world's content;
the boundary is set one notch tighter than the first successful reconstruction. R writes the
definition into 13 or 03 as normative.

### Capabilities and automation (OD-28 – OD-31)

#### OD-28 · Self-approval friction
**Status:** open. **Touches:** 05 §9 (the gate as honesty instrument); 02 §5; 06; 12 W3.
**Question:** A hand-written send by the operator still stages at the Queue — the gate checks
holes, consent, caps, not just delegation. One gate, one story; but the operator approves their
own work on every outbound act.
**Options:** full Queue row always (uniform, auditable; ritual friction on the most personal
acts) vs a compressed same-surface confirm for hand-driven exits that still runs every check
(fast; a second gate form to learn, and the first crack in "ONE queue") — see OD-57 for the
boundary rule this would need. **Evidence:** M — median hand-send-to-self-approve latency and
the rate at which gate checks actually catch something on hand-written sends. If the catch
rate is meaningfully non-zero, the full row has earned its friction; if it is ~0 over a pilot
month, compress.

#### OD-29 · Delegability of mounted-app surfaces
**Status:** open. **Touches:** 05 §10; 07 §8; 03 §4.3; 06.
**Question:** Room-backed (mounted-app) verbs got the full drive slider including hand-off —
but can a worker genuinely drive an arbitrary built app's bespoke surface, or only its declared
verbs? A mounted app whose value *is* its UI may resist hand-off in practice.
**Options:** verbs-only delegation (honest, enforceable; hand-off quietly weaker for exactly
the richest apps) vs full-surface drive (the promise kept; failure modes multiply with every
bespoke UI). **Evidence:** P — a worker drives three real built apps end-to-end via verbs
only; measure task completion. Where verbs-only completes >90% of the app's actual jobs,
declare verbs-only openly in 05 and stop implying more.

#### OD-30 · Who classifies a "material change"
**Status:** open. **Touches:** 05 §6 (autonomy resets); 06 §5; 17 §6.
**Question:** A material recipe/template edit drops the affected class one autonomy notch with
a visible notice. Who decides "material" — any diff (predictable, resets constantly, punishes
polish), semantic judgment by the system (right in spirit; the trust machinery now depends on
a model's opinion of itself), or operator self-declaration (sovereign; invites silent
under-declaration)?
**Proposed:** any-diff-resets as the default, with a same-surface "cosmetic — keep autonomy"
override the operator can mark, logged in the ledger. Errs toward trust; the override keeps it
livable and every override is auditable.
**Evidence:** M — reset frequency and override rate over a pilot; O — after any incident:
"would the one-notch drop have caught this?" R ratifies or replaces.

#### OD-31 · Batch — ladder rung or presentation preference
**Status:** open. **Touches:** 05 §7; 02 §5; 12 W3.
**Question:** Is "batch" a distinct autonomy-ladder position or a Queue presentation preference
of "propose"? 05 kept it a rung because pacing-of-approval is experientially distinct; the two
could collapse without violating the constitution.
**Evidence:** M — approval-latency distributions for batch vs propose classes; if they are
statistically indistinguishable, collapse the rung and simplify the ladder. O — "do you think
of these as different promises?"

### Missions and standing orders (OD-32 – OD-34)

#### OD-32 · The errand/mission judgment line
**Status:** open. **Touches:** 06 §3; 13; 17 §4 (calibration).
**Question:** Single-step delegated errands auto-judge against the session criteria bar with
override; multi-step missions get an operator verdict. Where exactly is the threshold — step
count, outbound exposure, spend?
**Proposed:** exposure, not steps: anything that touched a counterparty or spent above its
stated floor gets an operator verdict regardless of step count.
**Evidence:** M — verdicts-per-day against verdict quality (dwell, overturn rate) in pilot;
the threshold is set where quality starts decaying — judgment fatigue is measurable, and the
line should sit just below it.

#### OD-33 · Remediation-mission starts as earnable autonomy
**Status:** open. **Touches:** 06 §5 (automations may only STAGE mission proposals); 05 §6.
**Question:** For watchdog-class remediation at high earned trust, is stage-only too
conservative — should "start a remediation mission" ever become an earnable autonomy class?
**Trade-offs:** stage-only means a 3 a.m. failure waits for morning (calm, slower recovery);
an earnable start-class heals overnight but crosses the deepest line in the model — work that
begins work. **Evidence:** M — pilot count of staged remediation proposals approved unchanged
(a long streak of rubber-stamped "yes, fix it" is the evidence the class could be earned);
O — "which of these would you have wanted started without you?" R — this touches the one-gate
story; the constitution's editor must sign it.

#### OD-34 · A budget rung for money-heavy mission starts
**Status:** open. **Touches:** 06 §3.5; 09 (ceremony ladder); 05 §9 (caps).
**Question:** One-gesture mission start authorizes the brief's stated budget; gates protect
exits. A plan that front-loads heavy metered spend *before* any gated exit spends real money on
one gesture. Does the ceremony ladder need a distinct budget-approval rung for money-heavy
starts, or do stated-budget-plus-caps suffice?
**Evidence:** P — money-heavy mission start with the spend curve rendered on the brief; O —
"what number on this screen would have made you want a second confirm?" M — pilot incidence of
missions whose pre-gate spend exceeded the operator's stated surprise threshold. If that
incidence is ~0, no rung.

### Artifacts and builders (OD-35 – OD-38)

#### OD-35 · Counterparty portals and the external namespace
**Status:** held (product decision, operating model §8.5 — the model supports, does not force).
**Touches:** 06 §6 (heartbeat/ROI honest enough to show, no surface designed); 07; 11 §8.3
(deliberately minimal `/shared/:token`); 16 §14 (Ledgers never travel with shared artifacts);
17 §11 (lessons sealed operator-only).
**Question bundle, held together so portals arrive as one design:** would a client see their
own automations' heartbeats and ROI; their campaigns' earned lessons (a consent design 17
deliberately does not preempt); per-entry Ledger visibility if collaborators ever gain artifact
access; and a full external IA document replacing 11's minimal namespace.
**Evidence when taken up:** O — client-side interviews ("what would seeing this do to the
relationship?"); business decision (R). Until then every document keeps the seal: honest
numbers, no external surface.

#### OD-36 · Shipped-app runtime outbound
**Status:** open. **Touches:** 07 §8 (deploys gate; vitals annotate); 05 §9; constitution §8
(one gate); operating model §8.5.
**Question:** When Jane's deployed booking app emails *her* customers confirmations, is that
outbound the counterparty's product (monitored, never gated — the operator's Queue would
otherwise become a bottleneck inside someone else's business) or must it ride the operator's
approval classes (one-gate story kept whole — nothing the platform runs sends without a
class)?
**Proposed:** the runtime boundary is the artifact frame: sends the *app* makes to *its own
users* under its declared templates are product behavior — monitored, heartbeat-traced,
template changes gated; sends addressed to anyone else, or off-template, ride the operator's
classes. **Evidence:** P — walk J7's app through a template change and an off-template attempt;
O — Jane: "whose email is this?"; R — the constitution's editor rules whether "one gate"
means one gate *for the operator's acts* (the proposed reading) or for every send the platform
executes.

#### OD-37 · Messages-as-artifacts at volume
**Status:** open. **Touches:** 07 §3 (every outbound piece framed); 03 (view-areas); 01 P3;
11.
**Question:** Framing every sent email gives drafts versioning, compare, and outcome
annotations — but should hundreds of routine sends each surface as artifact rows in
view-areas, or stay framed-on-demand from their threads (the Thread as home view, inventory
only on ask)?
**Proposed:** framed-on-demand; the frame is a property of the message, not a row it owes any
area. **Evidence:** P — a view-area seeded with 300 sent messages: can operators still find
the five artifacts that matter (P3's test)? M — row-interaction rate on routine sends (if ~0,
they were noise).

#### OD-38 · Snapshot minting for living artifacts
**Status:** open. **Touches:** 07 §5; 04 (research maps); 06 (automation-fed datasets).
**Question:** Named snapshots at meaningful moments (cited by a report, promotion, manual pin)
replace authored versions for living artifacts — but the automatic trigger list and retention
rhythm are undecided, and they decide whether the rails stay legible.
**Evidence:** P — render a research map's rail after a simulated year under three trigger
policies (citation-only; citation+monthly; citation+every-promotion+monthly); operators must
answer "what changed since the report?" in under a minute. The sparsest policy that passes
wins.

### Portfolio, creation, and lifecycle (OD-39 – OD-46)

#### OD-39 · A continuous book-of-business surface
**Status:** open. **Touches:** 08 §3 (one row kind per lens; close-won as the graduation seam);
02 §7; 10 J1.
**Question:** An operator watching a deal cross the prospect→client seam may want one
continuous surface spanning records and worlds — which requires relaxing the single-row-kind
rule that keeps every lens honest.
**Options:** keep two lenses with a hand-off affordance at the seam (rows stay honest; the
crossing moment is two places) vs a mixed-kind book-of-business lens (one story; every bulk
action and roll-up now needs per-kind rules). **Evidence:** O — sit with an operator the week
a deal closes: do they actually flip between the two lenses, or does the Brief's narration of
the crossing suffice? M — cross-lens navigation frequency within an hour of close-won events.

#### OD-40 · Propagation to derived custom views
**Status:** open. **Touches:** 08 §6; constitution §11 (inheritance); 09 §10.
**Question:** Built-in lenses receive genome-layer improvement proposals; saved customs derived
from them stay frozen and will drift at scale (the pipeline gains a Review column; your copy
never hears about it).
**Options:** frozen customs (predictable, drift accepted) vs diff-shown adopt-proposals to
derived customs, exactly the §11 grammar applied to views. **Proposed:** the latter — views
are dressing, and §11 already solved dressing inheritance. **Evidence:** M — at month six of a
pilot, count customs missing improvements their base adopted; O — show the diff: "did you want
this?" R ratifies.

#### OD-41 · Money roll-up strictness
**Status:** resolved — **ratified as 08 wrote it.** Sums of the operator's own ledger lines
(what clients owe the operator) may total across counterparties; counterparty-side content
never aggregates. Constitution §13's clause governs *counterparty data*; the operator's
receivables are the operator's own business data about their own business. The stricter
reading (forbidding even own-ledger totals) would break the Money lens for exactly its core
question and protect no confidence. 08 stands; no edits owed.

#### OD-42 · Rung-3 isolation review — one screen, two marks
**Status:** open. **Touches:** 09 §6 (one-screen reading); constitution §11 ("proposal +
isolation review"); 12 W5; 13.
**Question:** 09 renders the isolation review as a section of the same one-screen Proposal
with a second acknowledgment mark, honoring "one screen, never a wizard." If the review was
meant as a genuinely separate step, 12's wireframes must overrule.
**Evidence:** P — comprehension test: after chartering at rung 3 via one-screen-two-marks, can
operators state the isolation contract unprompted ("Dana's data stays in Dana's world; sends
run under her name")? ≥90% pass keeps one screen; below that, the separate step earns its
ceremony. This is the rare case where *more* ceremony may be the honest choice.

#### OD-43 · Grant ceremony at portfolio scale
**Status:** open. **Touches:** 09 §7 (grants never batch); constitution §11 (never silently
inherited); 08; 10 J2 (n=50).
**Question:** Per-world grant ceremony ×50 for a legitimately shared operator-owned credential
(one brokerage MLS login) — right friction, or does it need a **grant template** the operator
pre-scopes to a setup class (each world still confirms, but the scope is pre-written)?
**Trade-offs:** strict reading keeps every grant a conscious act; ×50 identical ceremonies
teach operators to stop reading exactly the screens isolation depends on. **Evidence:** P —
simulate the 50-agent rollout; M — per-grant reading time by ordinal (if grant #30 is
confirmed in under two seconds, the ceremony has already failed and the template is *safer*);
R — constitution's editor rules whether a pre-scoped template violates "never silently
inherited" (it arguably honors it: the template is explicit, the confirm survives).

#### OD-44 · Signed-but-uncharted — where interim work lands
**Status:** open. **Touches:** 09 §8 (Queue aging covers the nag); 10 J1/S3; 08 (the seam);
06.
**Question:** Close-won stages a spawn proposal the operator doesn't charter for days while
real client work (replies, payments) arrives. Where does it land — held at the seam on the
prospect record (nothing enters a world that doesn't exist; a growing pile with no Desk) or
in the agency world (workable now; must migrate on charter, and agency memory absorbs
client-scoped material it will have to un-learn)?
**Proposed:** held at the seam, rendered as the spawn proposal's own accumulating "arrived
while you decide" list — the pile *is* the nag.
**Evidence:** P — S3 journey test with three days of simulated arrivals; O — "where did you
expect that payment to be?"

#### OD-45 · Decline semantics over time
**Status:** open. **Touches:** 04 §9.3 (offers back off until the signal threshold doubles);
09 §10 (declining is remembered; re-parenting declines); 10 J2 (7 skipped improvements); 08.
**Question:** What does "no" mean over time, uniformly? Declined improvement proposals,
declined re-parenting (permanent sibling-orphans that never receive adopt-proposals), declined
world proposals — permanent suppression, or re-offer on materially stronger evidence?
**Proposed:** generalize 04 §9.3's rule as the platform-wide decline grammar — a decline backs
off every surface at once and the same proposal never re-offers until its evidence has
materially strengthened (threshold doubled or equivalent); nothing escalates, nothing nags.
**Evidence:** M — the two failure rates: annoyance (declines of re-offers) vs regret
(operators manually adopting later what they once declined). The grammar is tuned until both
are low; regret is the more expensive error for a mastery product. R writes it into one
owning document (09).

#### OD-46 · Seed identity at spawn
**Status:** resolved — **ratified: 10's reading.** The mid-funnel demo site mounts as draft v1
of the client's real artifact — never a copy — matching the promotion doctrine ("never an
export, never a copy") applied to spawn. A snapshot-copy would fork provenance at exactly the
moment continuity matters most: the client is buying the thing they saw. 09's seed rules owe
the one-line statement (editorial queue); sibling-derived seeds still cross as scrubbed
slot-bearing templates per 09 §10 — the two rules coexist because the demo was built *for*
this counterparty, the sibling patterns were not.

### Ceremony readings and mastery loops (OD-47 – OD-51)

#### OD-47 · Charter-before-grants (the 60-second bound)
**Status:** resolved — **ruled: 13's reading is binding.** Constitution §11's "proposal +
connection grants" names the rung's *contents*, not its ordering. Charter completes first;
grant asks stage as the Desk's first moves; an OAuth-style flow never blocks birth (it cannot
physically fit the 60-second bound, and the anticipation doctrine wants the world to exist and
ask, not a wizard to gate). Work needing an unfulfilled grant holds visibly at the gate. 13's
AT stands; 09 already conforms; the constitution's line needs no amendment, only this recorded
reading.

#### OD-48 · The predict-first affordance
**Status:** open. **Touches:** 13 (critique calibration measurable); 16 §9; 17 §4.
**Question:** "Judgment transfers" is only measurable if the operator scores before the system
reveals. Is predict-first a mandatory workshop feature or an opt-in ritual?
**Trade-offs:** mandatory makes calibration honest and universal but inserts a quiz into a
craft tool (the fastest way to make mastery feel like homework); opt-in preserves flow and
biases the measurement toward the already-diligent.
**Evidence:** P — both variants over a two-week workshop cycle; M — calibration hit-rate
improvement *and* session abandonment. If opt-in adoption exceeds ~40% naturally, opt-in wins;
if it languishes at 5%, mandatory-but-occasional (sampled, not every critique) is the
compromise to test next.

#### OD-49 · Cross-world calibration aggregation
**Status:** open. **Touches:** 17 §8 (kept per-world, per-craft, operator-private); 01 P11;
05 §6 (autonomy defaults).
**Question:** Should the operator's calibration record ever aggregate across crafts/worlds
into a portfolio-level judgment profile that tunes staged-move and autonomy defaults?
Aggregating *operator* data is arguably safe under P11 (it is not counterparty data) but was
never authorized by the constitution.
**Evidence:** R first — the constitution's editor rules whether P11's seal covers operator
self-data (proposed: it does not; the operator may aggregate themselves); then O — explicit
opt-in framing ("use what the system knows about your judgment everywhere?"); then M — do
profile-tuned defaults measurably reduce overturns? Ship only if all three pass, in that
order.

#### OD-50 · The scrub generalization floor
**Status:** open, provisionally bound. **Touches:** 17 §7 (single-counterparty figures promote
only as ranges or defaults-to-confirm); 09 §10; 13 (scrub tests).
**Question:** When does a numeric figure count as safely aggregate-derived? **Provisional
binding, per 13 §0's threshold style: n ≥ 3 contributing worlds**, below which a figure
promotes only as a range or a default-to-confirm.
**Evidence:** R — engineering/privacy review before implementation: adversarial
reconstruction test (given the promoted figure and public knowledge, can any single
counterparty's number be recovered at n=3?). The floor rises until the answer is no. This
entry revises the number; documents cite the entry, not the number.

#### OD-51 · Domain-pack curation
**Status:** held — platform-ops, outside this phase's reach. **Touches:** 17 §3 (delivery
mechanics are specified: versioned packs, diff-shown adoption).
**Question:** Who curates built-in packs, from what sources, at what cadence — and whether
pack curation becomes an in-product capability. **Evidence when taken up:** ops decision (R)
plus M on pack-adoption and pack-override rates as the demand signal.

### Wireframe and presentation (OD-52 – OD-55)

#### OD-52 · Queue email compare semantics
**Status:** resolved — **ruled.** The approval compare for an email is **before/after**: this
draft against the last thing actually sent to that recipient/class (plus the edit-diff on
edit-then-approve), matching 02's publish reading. Variant-vs-variant compare is the producing
session's job and stays reachable through the item's provenance link to that session. 12's
rendering stands; 02 gains the one-line email extension (editorial queue).

#### OD-53 · Loading honesty
**Status:** resolved — **ruled.** A last-verified value may render during recompute **with an
explicit age/skew stamp** ("as of 09:12"). No-theater forbids *fake freshness*, not
stale-marked truth; render-nothing is required only where no verified value has ever existed.
An unstamped stale value anywhere is a defect. 12's Pulse/Face loading states stand as drawn.

#### OD-54 · Visual-pass deferrals
**Status:** held — bundled for the visual-design phase; the ASCII dockings in 12 are
explicitly non-binding. Contents: panel geometry (Queue ~440px right-dock; Explore 2/3+1/3;
Palette-left/Counsel-right vs bottom-drawer Counsel on narrow desktops); the Field's orb form
at compressed tiers (orbs vs name-glyph rows, and at which count); genome accent-token bounds
(how much visual identity before the portfolio stops reading as one product — 03 §6.1.10);
a minimal posture glyph in the context header inside workshop sessions; physical keybindings
(semantic bindings are binding; Cmd+O/Cmd+. physical keys swappable pending a keymap audit).
**Evidence:** the visual pass itself, plus P on any variant that changes information
availability rather than styling.

#### OD-55 · Terminology ratification bundle
**Status:** open. **Touches:** 12 §0.2 (the label table); 07 (Builder display name); 13
(scenario-4 naming); constitution §2, §4; 02 §12.2.
**Question:** One ratification sweep for the terminology-skin owner (doc 15's author):
(1) 12 §0.2's calls — "History" (Ledger), "Run history", "Plan", "Commit" — ratify or
override; (2) the Builder skinned as "Site Studio"/"App Studio" per the workshop table vs a
reserved cross-world word like Mission/Queue (the vision documents' "builder" identity argues
for reserving it; the workshop grammar argues it is a craft like any other — **proposed:**
keep Studio skinning; "Builder" remains a spec word); (3) "Design Studio" vs "Collection
Studio" for the apparel craft (13's flagged dispute); (4) constitution §4's illustrative chip
"Prospect pipeline" vs the shipped "Prospects" (amendment queue); (5) the onboarding line
"…and I'll make it a world" vs display-name purity; (6) whether quoted UI strings may use
generic lowercase "routine" in worlds without the Routine skin.
**Evidence:** string audit (12 §11.4 already enforces part), then O — label comprehension test
on the contested pairs with operators who have never seen the spec words.

### Structural tensions and validation (OD-56 – OD-59)

#### OD-56 · Provisional workshop vs loose session
**Status:** open. **Touches:** 16 §12 (one-confirm provisional workshop-area); 01 P5 (zero
decisions to start); 03 §4 (areas are the only structure slot); 11 §6.
**Question:** A first-time craft act in a world with no matching workshop-area currently costs
one confirm (composing a provisional workshop creates an area). 16 considered and rejected a
lighter "loose session held on the Desk until it earns an area" as contradicting 03's area
rules — but the confirm is still a decision in front of a first craft act, in tension with P5.
**Evidence:** M — funnel: abandonment at the confirm for first-time craft acts (if operators
bounce there, the loose-session variant deserves its trial despite the structural cost);
P — both variants on journey J7's first build beat. If the confirm shows no measurable drag,
16's ruling stands and this entry closes.

#### OD-57 · The confirm/gate boundary
**Status:** open — a tension no single document owns. **Touches:** every document; sharpest in
02 §5, 04 §11.5, 05, 06 §3.5, 09, 16 §12.
**Question:** The design now contains one Queue gate and a growing family of one-confirm
ceremonies (fold-in, provisional workshop, mission start, watch mount, adopt-proposals,
charter itself). No document states the criterion for which acts are ceremony-confirms and
which are Queue-gated approvals — and OD-28's compressed-confirm option would blur it further.
**Proposed rule:** **gates protect exits, confirms protect structure.** Anything that leaves
the operator's boundary — sends, spends, publishes, touches a counterparty — walks the Queue,
no exceptions; anything that rearranges the operator's own space and is undoable takes at most
one same-surface confirm. Every current surface conforms; writing it down keeps it true.
**Evidence:** R — adopt into the constitution (amendment queue candidate); then P — audit
every confirm site in 02–12 against the rule and red-team for exits dressed as confirms.

#### OD-58 · The proposal-pressure budget
**Status:** open — a tension no single document owns. **Touches:** 04 §9.3 (promotion offers);
05 (autonomy offers); 06 (mission proposals); 08/09 (adopt-proposals, renewal proposals);
03 §2.5.2 (vital swaps); 17 (mastery reps, pack adoptions); OD-19's digest would add another.
**Question:** Seven-plus machines now generate proposals toward the same operator. Each is
individually quiet and evidence-linked; nothing owns the *sum*. At 50 worlds the Desk and
Brief risk becoming proposal inboxes — the anticipation doctrine inverted into a to-do list
the system writes for you, eroding P3 and the no-theater feel from the aggregate side.
**Proposed:** a portfolio-level pacing rule owned by 02 (the Brief already ranks by
attention): proposals compete for a bounded number of slots per Brief/Desk render, ranked by
evidence strength; losers wait, never stack.
**Evidence:** M — proposals-surfaced-per-week per operator and acceptance rate by source in a
50-world pilot (a falling acceptance rate is the saturation signal); O — "does the system feel
like it wants things from you?" — any yes is a failing grade.

#### OD-59 · Threshold and validation register
**Status:** open — numbers binding until this entry revises them (13 §0). Each with its
validating instrument: the Field's attention band size at 100 worlds (P: 100-world seeded
portfolio, find-the-sick-world test); AT-09's two-minute re-orientation bound (P: S7 script);
AT-10's one-missed-heartbeat detection window (M: false-alarm vs missed-failure rates); the
5s/60s creation timers (P: instrumented creation runs at rungs 1–3); 08 §3.2's 60-day
prospect aging-out default (R: the funnel-owning genome doc confirms or overrides; O: "when
is a silent prospect dead to you?"); 04 §9.2's two-week/six-week decay clocks (validated as
the contract by OD-08; P re-checks the feel at pilot). Revisions land here first, then in the
owning documents.

---

## 2. Resolved during review

Items the fix wave settled inside the documents; recorded so nobody re-opens them:

| Resolution | Where it lives |
|---|---|
| The seven-scenario enumeration and S↔J mapping table | 01 (mapping column); 10 Appendix A |
| Cast canon: **Marco's Murals**, **Thread & Stone**, **Dave Kowalski** (10 Appendix A is canonical); Rossi Plumbing = sanctioned independent exemplar (13 §0 footnote) | 10, 13 · sweeps still owed: §4 |
| Mom's world kind = family **Business**; near-miss re-grounded on her own sellers' data | 10 §2.x; 17 §7.2 |
| Site/App = **workshop-area** (the deep-artifact exception); builder sessions hang under it; two named session kinds (strategy · build), one shelf, one Ledger | 03 §4.3/§7.1, 11 §6.2, 12 W8, 16 §5/§13.3 |
| Promotion-offer surfaces enumerated (four, same offer, same rows) | 04 §9.3 |
| Reference-listing rule (operator-owned sibling material metadata-only; counterparty-bound never lists) | 16 §4.2 (open halves → OD-27) |
| Fold-in verb (operator-gesture merge, one confirm, undoable) | 04 §11.5 |
| Reply-approval items are class 2 (absence blocks running work), with the disambiguation sentence | 03 §3.2.3 |
| Artist-world ceremony = rung 2, priced live from its Proposal rows | 03 §7.3, 09 §5–6 |
| Posture rides the interpretation chip as the dial glyph, never the word | 02 §3.2, 03 §5.1 |
| "History" is the Ledger's display name everywhere rendered | 02 §3.5, 12 §0.2 |
| Creation chips use display names ("→ new Exploration"), never "new world" | 02 §3.2 |
| Pulse count reconciled to the Brief's five approval-class items | 12 §1.3 |
| AT-16/17/18 added; AT-08 gauntlet extended (schema migration, payments grant, live-error fix) | 13 |

## 3. Constitutional amendment queue

One-line amendments awaiting the constitution's editor; until landed, the constitution's
letter wins and the affected documents cite the OD entry:

1. **§5 area band** — "up to 7 visible; operating genomes charter with 3–5; newborn/curiosity
   worlds may show fewer" (OD-16).
2. **§5 default landing** — name the curiosity-world map-landing carve-out (OD-09), if the
   evidence upholds it.
3. **§6 drive modes** — enumerate all four (hand · asked-for-once · handed-off · automated);
   the text currently lists three and mentions "automated" separately, which has already
   caused downstream miscounts (06's fix wave).
4. **§6 bench cap** — name the deep-artifact two-session-kind exception 16 §5 defines (the
   only sanctioned second session kind; no third, no third bench).
5. **§4 lens chip example** — "Prospect pipeline" → "Prospects" per 08/12 canon (OD-55.4), or
   mark the list explicitly exemplary.
6. **§11 rung 3** — optional clarifying clause per OD-47's ruling (ceremony names contents,
   not ordering); no change strictly required.
7. **Candidate:** the confirm/gate boundary rule (OD-57), if adopted.

## 4. Editorial propagation queue

Cross-document edits owed for already-made decisions to bind system-wide. Owner → edit
(source). Not disputes; unlanded propagation.

- **01:** qualify P11's "moves freely" to gate-promoted+scrubbed travel plus adjacency
  pointers (OD-11) · confirm P14's timing reading — signal at occurrence two, proposal at
  three (05 §5.2's rule).
- **02:** rewrite §3.3 rule 3 to the switcher reading (OD-14) · §7.1 "three bands" → four ·
  own the non-operator access rule with 08 (non-operators interact only via `/shared` pages
  and the conversation — 10's rule) · one-line email-compare extension (OD-52).
- **03:** conversation-surface anatomy section (OD-01) · §2.1.5 grant rows carry expiry dates
  (06 §3.4) · §7.2 document-workshop/Paperwork craft entry (07) · §7.3 one-line wrong-genome
  beat acknowledgment (10 J3) · §7.4 re-stage "your brother granted three pieces" per the
  grant-actor rule, absorb J4 §4.3's commerce substrate, and run the ①/② precedence
  consistency pass · §7.5 Support-inbox extracted-records area (05 §5.2) · §7 dressing catalog
  family-business Money entry (09 §12.1) · §4.3 name the mounted-app body (view-area whose
  body is the artifact's own interface — 05's chosen reading) · §7.6 metered-spend echo
  (04 §7).
- **04:** §9.3 explicit "sleeps with its world" watch sentence (OD-10) · §16 correct the
  "S6 and S7 as full journeys" cite (OD-03).
- **05:** register fold-in in the verb catalog (04 §11.5) · state "first contact = any
  never-before-contacted recipient" in §8.3 (OD-06) · echo the genome-starter-recipe rule
  (16 §13.5).
- **06:** billing subsection — issuance-automation grammar, drafts staging at the Queue, the
  payment-collection grant sentence (07 §5) · absorb J5a's fail-closed rule (lost reply
  visibility pauses follow-up rungs, files to Queue) · cross-reference the retirement ceremony
  once 09 owns it.
- **07:** sweep "Theo" → Marco (lines ~129, ~408) · state the grant-actor rule in §7.2
  (grants are operator-approved acts of the owning world).
- **08:** §8.4 reword to "offer thresholds transfer, standing grants do not" (05 §8.1) · §5
  cross-cite 07 §7.2's set-grant granularity · add the adoption-state field and the
  setup-family lens · §4's retrievable-contents list adds explorations/beacons/discoveries.
- **09:** own the world-retirement/offboarding ceremony (10 §1.7 walks it inline; the owning
  section belongs here, automations-retired-first) · §4.7 grant grammar gains expiry metadata
  and the supplier/print-vendor + backend-provisioning examples (07) · §7 intake example shows
  the full connect loop (11 §8.3) · §10 correct "5 areas, 3 automations" to Appendix A's
  four-area derived-agent block; reconcile Harbor Dental's internal drift (§4.4 vs §6; 12 W5)
  · §11 learned-setup contents add genome-mounted apps as catalog-versioned layers (05 §10.1)
  · sweep Dana → Dave Kowalski (10 Appendix A canon) · state OD-46's seed ruling.
- **10:** J2 paperwork beat (fill → Queue → `/shared` sign → signed outcome) · J2.1 isolation
  acknowledgment + scrubbed-slot seed clause (09 §10) · J4.1 cite the rights mark (07 §2.6) ·
  J4.5 replace the illegal greyed counterparty listing (16 §4.2 — clean miss + Counsel
  pointer) · J7 schema-change + live-migration beat (07 §8.5) · publish the reciprocal S↔J
  table; settle J6's subject per OD-03.
- **11:** §6 deep-artifact session-ownership rule (first build mounts the area via 16 §12) ·
  §8.3 add the signer-facing signature page, the selection-enabled review page, and the
  review-link sign-off/request-changes affordance (07 §2.4–2.5, 08 §3.1) · §10 add the
  exploration/Map-area/snapshot three-way reconciliation entry · §6.3 note the
  curiosity-landing exception (02 §7.2).
- **12:** conversation frame (OD-01) · W8 render Waiting/Approved and draw the data region
  (07 §8.5) · W10 per-row "shouldn't have fired" action (03 §7.5) · W5 criteria-pack
  provenance clause (09 §4.2) and an adopt-proposal item variant · W1 switcher "address,
  don't go" row action (OD-14).
- **13:** re-verify AT-04/AT-15 against Mom's family-Business reading · add the sideways
  re-genome/de-isolation AT (09 §13 check 15 states the pass condition) · formalize the
  extended scrub test (17 §10.7) · adopt the fold-in acceptance check (04 §16.9).
- **16:** §13.2 name the cold-outreach send class (OD-06).
- **17:** sweep "Inkfall" → Thread & Stone (six occurrences).

## 5. Evidence instruments

Every open entry names how it closes, using four instruments: **P** — clickable-prototype
tests with scripted scenarios (the journey scripts in 10 and the acceptance bars in 13 are the
test material); **O** — operator and counterparty interviews with the specific questions
written in the entry, asked against real or staged screens, never in the abstract; **M** —
instrumented pilot metrics with the decision threshold stated in advance (an entry that cannot
say what number would change the ruling is not ready to cite M); **R** — a ruling by the
owning authority (constitution's editor, engineering phase, platform ops, or this register
itself). Entries resolved by R alone are marked so deliberately: no prototype would change
them, and pretending otherwise is its own kind of theater.

## 6. Editorial log

Small mechanical corrections applied directly, logged for the record:

- **09 §4.4 cite** — "(06 §14)" corrected to "(06 §4, mechanism 14)": doc 06 has no §14; the
  charter-time naming rule is mechanism 14 in 06 §4's enumeration.
- **02 §4.2 cite** — "three push rules (§8)" corrected to "(§9)": §9 is the notifications
  section; §8 is Continue/resume.

---

*A threshold in 13 is binding until this file revises it with evidence (13 §0). A dispute that
is argued anywhere else — a commit message, a local footnote, a quiet divergence — does not
count as raised. Entries are append-only: OD numbers are anchors and never renumber.*
