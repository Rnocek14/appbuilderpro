// Client-side re-export of the injection-quarantine core (house pattern: the one implementation
// lives in supabase/functions/_shared/untrustedText.ts, shared with every worker seam).
export * from '../../../supabase/functions/_shared/untrustedText';
